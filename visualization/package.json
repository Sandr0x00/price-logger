{
  "private": true,
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "bootstrap": "^4.3.1",
    "d3": "^5.12.0",
    "eslint": "^6.5.1",
    "jquery": "^3.4.1",
    "lit-element": "^2.2.1",
    "navigo": "^7.1.2",
    "node-sass": "^4.12.0",
    "popper.js": "^1.16.0",
    "rollup": "^1.24.0",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-terser": "^5.1.2"
  },
  "scripts": {
    "test": "eslint .",
    "build:css": "node-sass sass -o public/css",
    "build:js": "rollup -c",
    "build": "npm run build:css && npm run build:js && ./copy.py && npm run test",
    "build:prod": "npm run build:dev && tar -czf visualization.tar.gz public views package.json package-lock.json server.js",
    "deploy:copy": "scp visualization.tar.gz visualization.service pi@raspberrypi.fritz.box:~",
    "deploy:install": "ssh pi@raspberrypi.fritz.box 'bash -s' < deploy.sh",
    "deploy:status": "ssh pi@raspberrypi.fritz.box 'systemctl status visualization'"
  }
}
